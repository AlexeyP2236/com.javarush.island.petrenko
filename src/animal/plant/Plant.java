package animal.plant;

import animal.Animal;
import title.NamesAnimal;

import java.util.HashMap;
import java.util.Map;

public class Plant extends Animal {

    {
        setAnimalName(NamesAnimal.PLANT);
        setWeight(1);
        setSpeed(-1);
        setMaxSaturation(-1);
    }

}
